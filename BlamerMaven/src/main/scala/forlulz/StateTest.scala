package forlulz

import su.msk.dunno.scage.screens.support.tracer.{State}

object StateTest extends Application {
  /*val state = new State("data1", "hello").put("data2", "world").put("hello", "peace!")
  new state.check {
    key("data1") {
      key("data2") {
        key(string("data1")) {
          println(string(string("data1"))+" "+string("data1")+" "+string("data2"))
        }
      }
    }
    key("data2") {
      println(3+string)
    }
  }

  val new_state = state.filter(str => {
    str == "data1" || str == "data2" || str== state.getString("data1")
  })
  println(new_state)*/
}